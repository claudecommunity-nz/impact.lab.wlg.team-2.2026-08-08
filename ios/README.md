# SwiftUI client (branch `3-swiftui`)

Separate native Location Picture app — **design TBD**.

Plugs into the Vapor API in `server/` (`docs/02-api-contract.md`, `docs/07-demo-data.md`).
Does not replace the Vite/React UI in `frontend/`.

No app sources yet. Scaffold after product/architecture plan is agreed.
